import { ScrolleventDirective } from './scrollevent.directive';

describe('ScrolleventDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolleventDirective();
    expect(directive).toBeTruthy();
  });
});
